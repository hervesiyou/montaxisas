import React, { Component } from 'react';
import LoginWelcome from '../components/LoginWelcome';

export default class LoginWelcome9Screen extends Component {
 
  render() {
    return (
       <LoginWelcome navigation={this.props.navigation}  />
    );
  }
}