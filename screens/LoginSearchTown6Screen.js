import React, { Component } from 'react';
import LoginSearchTown from '../components/LoginSearchTown';

export default class LoginSearchTown6Screen extends Component {
 
  render() {
    return (
       <LoginSearchTown navigation={this.props.navigation}  />
    );
  }
}