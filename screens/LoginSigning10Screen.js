import React, { Component } from 'react';
import LoginSigning from '../components/LoginSigning';

export default class LoginSigning10Screen extends Component {
 
  render() {
    return (
       <LoginSigning navigation={this.props.navigation}  />
    );
  }
}