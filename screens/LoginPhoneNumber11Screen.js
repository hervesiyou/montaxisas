import React, { Component } from 'react';
import LoginPhoneNumber from '../components/LoginPhoneNumber';

export default class LoginPhoneNumber11Screen extends Component {
 
  render() {
    return (
       <LoginPhoneNumber navigation={this.props.navigation}  />
    );
  }
}