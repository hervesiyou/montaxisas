import React, { Component } from 'react';
import LoginChangeNumber from '../components/LoginChangeNumber';

export default class LoginChangeNumber8Screen extends Component {
 
  render() {
    return (
       <LoginChangeNumber navigation={this.props.navigation}  />
    );
  }
}