import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    containers: {
        flex: 1,
        padding: 20
    },
    textInput: {
        borderRadius: 10

    }
})

export {styles}