import { StyleSheet } from "react-native";

 const styles = StyleSheet.create({
    container:{
        flex: 1,
        marginTop: 20,
        backgroundColor: "#FFF301"
    },
    textView: {
        color: 'white',
        fontSize: 40,
        fontWeight: 'bold'
    }
})
export {styles}